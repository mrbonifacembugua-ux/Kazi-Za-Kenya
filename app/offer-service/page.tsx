"use client";
import {FormEvent,useMemo,useState} from "react";import {useRouter} from "next/navigation";import {supabase} from "../../lib/supabase";
const MAX_BYTES=5*1024*1024,ALLOWED_TYPES=new Set(["image/jpeg","image/png","image/webp"]);type Geo={latitude:number;longitude:number;accuracy:number};
function ext(f:File){return f.type==="image/png"?"png":f.type==="image/webp"?"webp":"jpg"}function valid(f:File){if(!ALLOWED_TYPES.has(f.type))return`${f.name}: use JPG, PNG or WebP.`;if(f.size>MAX_BYTES)return`${f.name}: image must be 5 MB or smaller.`;return""}
export default function OfferServicePage(){const router=useRouter();const[profilePhoto,setProfilePhoto]=useState<File|null>(null),[workPhotos,setWorkPhotos]=useState<File[]>([]),[submitted,setSubmitted]=useState(false),[saving,setSaving]=useState(false),[error,setError]=useState(""),[geo,setGeo]=useState<Geo|null>(null),[locating,setLocating]=useState(false),[locError,setLocError]=useState("");const profilePreview=useMemo(()=>profilePhoto?URL.createObjectURL(profilePhoto):"",[profilePhoto]);const workPreviews=useMemo(()=>workPhotos.map(URL.createObjectURL),[workPhotos]);
 function locate(){setLocError("");if(!navigator.geolocation){setLocError("This device does not support location sharing.");return}setLocating(true);navigator.geolocation.getCurrentPosition(p=>{setGeo({latitude:p.coords.latitude,longitude:p.coords.longitude,accuracy:p.coords.accuracy});setLocating(false)},e=>{setLocError(e.code===1?"Location permission was not granted. Your typed area will still be saved.":"We could not get your current location. Your typed area will still be saved.");setLocating(false)},{enableHighAccuracy:true,timeout:15000,maximumAge:0})}
 function parsePrice(v:string){const a=v.match(/[\d,]+/g)?.map(x=>Number(x.replace(/,/g,""))).filter(Number.isFinite)||[];return{priceFrom:a[0]??null,priceTo:a[1]??null}}
 async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();if(saving)return;setSaving(true);setError("");for(const f of [...(profilePhoto?[profilePhoto]:[]),...workPhotos]){const v=valid(f);if(v){setSaving(false);setError(v);return}}const form=new FormData(e.currentTarget),name=String(form.get("name")||"").trim(),service=String(form.get("service")||"").trim(),description=String(form.get("description")||"").trim(),area=String(form.get("area")||"").trim(),price=String(form.get("price")||"").trim(),availability=String(form.get("availability")||"AVAILABLE");const{data:authData,error:authError}=await supabase.auth.getUser();const user=authData.user;if(authError||!user){setSaving(false);router.push("/login?next=%2Foffer-service");return}const uploaded:string[]=[];try{let photoUrl:string|null=null;if(profilePhoto){const path=`${user.id}/profile/${crypto.randomUUID()}.${ext(profilePhoto)}`;const{error:e}=await supabase.storage.from("portfolio").upload(path,profilePhoto,{contentType:profilePhoto.type,upsert:false});if(e)throw e;uploaded.push(path);photoUrl=supabase.storage.from("portfolio").getPublicUrl(path).data.publicUrl}const{data:existing}=await supabase.from("profiles").select("role,profile_photo_url").eq("id",user.id).maybeSingle();const payload:Record<string,unknown>={id:user.id,full_name:name,area,bio:description,role:existing?.role==="customer"?"both":existing?.role||"both",is_active:true};if(photoUrl)payload.profile_photo_url=photoUrl;if(geo){payload.latitude=geo.latitude;payload.longitude=geo.longitude;payload.location_accuracy_m=geo.accuracy;payload.location_updated_at=new Date().toISOString()}const{error:pe}=await supabase.from("profiles").upsert(payload,{onConflict:"id"});if(pe)throw pe;const prices=parsePrice(price);const{error:se}=await supabase.from("services").insert({provider_id:user.id,title:service,category:service,description,price_from:prices.priceFrom,price_to:prices.priceTo,availability_status:availability==="BUSY"?"busy":"available"});if(se)throw se;for(let i=0;i<workPhotos.length;i++){const f=workPhotos[i],path=`${user.id}/work/${crypto.randomUUID()}.${ext(f)}`;const{error:ue}=await supabase.storage.from("portfolio").upload(path,f,{contentType:f.type,upsert:false});if(ue)throw ue;uploaded.push(path);const url=supabase.storage.from("portfolio").getPublicUrl(path).data.publicUrl;const{error:ie}=await supabase.from("portfolio_items").insert({provider_id:user.id,title:`${service} work ${i+1}`,description:`Proof of work for ${service}`,storage_path:path,photo_url:url});if(ie)throw ie}setSubmitted(true)}catch(err){if(uploaded.length)await supabase.storage.from("portfolio").remove(uploaded);setError(err instanceof Error?err.message:"We could not create your worker profile. Please try again.")}finally{setSaving(false)}}
 if(submitted)return <main className="page"><section className="card success"><div className="mark">✓</div><h1>Your worker profile is ready</h1><p>Your service and portfolio are connected to your account{geo?", and your private device position is available for nearby-job matching":""}.</p><button onClick={()=>router.push("/")}>Back to Kazi za Kenya</button></section><style jsx>{styles}</style></main>;
 return <main className="page"><section className="card"><button className="back" type="button" onClick={()=>router.push("/")}>← Back</button><div className="brand">🇰🇪 Kazi za <span>Kenya</span></div><h1>Offer your service</h1><p className="intro">Create a clear worker profile. You may also share your device position for accurate nearby-job matching.</p><form onSubmit={submit}><label>Your name<input required name="name" placeholder="e.g. Peter Kamau"/></label><label>Service or category<input required name="service" placeholder="e.g. Plumbing, painting, cleaning"/></label><label>About your service<textarea required name="description" rows={5} placeholder="Tell customers what you do"/></label>
 <div className="locationBox"><b>📍 Worker location</b><p>Optional. Share your current device position to calculate nearby jobs accurately. The precise coordinates are stored for matching and are not printed on your public profile.</p><button className="locate" type="button" disabled={locating} onClick={locate}>{locating?"Finding location…":geo?"✓ Location captured — update it":"Use my current location"}</button>{geo&&<small className="ok">Position captured · accuracy about {Math.round(geo.accuracy)} m</small>}{locError&&<small className="locError">{locError}</small>}</div>
 <div className="two"><label>Area<input required name="area" placeholder="e.g. Kilimani"/></label><label>Starting price or price range<input required name="price" placeholder="e.g. From KSh 1,500"/></label></div><label>Availability<select required name="availability" defaultValue="AVAILABLE"><option value="AVAILABLE">Available for work</option><option value="BUSY">Currently busy</option></select></label><label>Profile photo <small>(JPG, PNG or WebP · max 5 MB)</small><input className="file" type="file" accept="image/jpeg,image/png,image/webp" onChange={e=>setProfilePhoto(e.target.files?.[0]||null)}/></label>{profilePreview&&<div className="profilePreview"><img src={profilePreview} alt="Profile preview"/><span>{profilePhoto?.name}</span></div>}<label>Proof of work / portfolio photos <small>(optional, up to 21 · max 5 MB each)</small><input className="file" type="file" accept="image/jpeg,image/png,image/webp" multiple onChange={e=>setWorkPhotos(Array.from(e.target.files||[]).slice(0,21))}/></label>{workPhotos.length>0&&<><div className="photoNote">📷 {workPhotos.length} portfolio photo{workPhotos.length===1?"":"s"} selected</div><div className="gallery">{workPreviews.slice(0,6).map((src,i)=><div className="thumb" key={src}><img src={src} alt={`Work preview ${i+1}`}/></div>)}</div></>}<div className="notice">Location sharing is optional. Your typed area remains available if you decline.</div>{error&&<div className="error">{error}</div>}<button className="submit" type="submit" disabled={saving}>{saving?"Saving profile and photos...":"Create my worker profile"}</button></form></section><style jsx>{styles}</style></main>}
const styles=`*{box-sizing:border-box}.page{min-height:100vh;background:#f3f6f3;padding:30px 16px;font-family:Inter,system-ui,-apple-system,"Segoe UI",sans-serif;color:#17221b}.card{max-width:720px;margin:auto;background:#fff;border:1px solid #dce4dc;border-radius:20px;padding:28px;box-shadow:0 12px 40px rgba(27,43,31,.1)}.brand{font-size:20px;font-weight:850;margin:8px 0 26px}.brand span{color:#15803d}h1{font-size:30px;margin:0 0 8px}.intro{color:#66736b;line-height:1.55;margin:0 0 24px}.back{border:0;background:transparent;color:#15803d;font-weight:750;padding:0;cursor:pointer}form{display:grid;gap:17px}label{display:grid;gap:7px;font-size:13px;font-weight:800}small{font-weight:500;color:#78847c}input,textarea,select{width:100%;border:1px solid #d5ddd6;border-radius:11px;padding:12px 13px;outline:0;background:#fff;font:inherit;color:#17221b}.two{display:grid;grid-template-columns:1fr 1fr;gap:14px}.locationBox{border:1px solid #cfe1d3;background:#f5fbf6;border-radius:13px;padding:14px}.locationBox p{font-size:12px;color:#607066;line-height:1.5;margin:5px 0 10px}.locate{border:1px solid #99c7a6;background:#fff;color:#156f37;border-radius:10px;padding:10px 12px;font-weight:800;cursor:pointer}.ok,.locError{display:block;margin-top:9px}.ok{color:#257342}.locError{color:#9f2020}.file{padding:10px}.profilePreview{display:flex;align-items:center;gap:11px;font-size:12px}.profilePreview img{width:58px;height:58px;object-fit:cover;border-radius:50%}.gallery{display:grid;grid-template-columns:repeat(3,1fr);gap:9px}.thumb{aspect-ratio:1.35/1;border-radius:10px;overflow:hidden}.thumb img{width:100%;height:100%;object-fit:cover}.photoNote{font-size:12px;color:#52705b}.notice{background:#eef8f1;border:1px solid #cfe8d6;border-radius:11px;padding:12px 14px;color:#386347;font-size:12px}.error{background:#fff1f1;border:1px solid #f0caca;color:#9f2020;border-radius:11px;padding:12px}.submit,.success button{border:0;border-radius:11px;background:#16803d;color:#fff;padding:13px 18px;font-weight:800;cursor:pointer}.submit:disabled{opacity:.65}.success{text-align:center;margin-top:10vh}.mark{width:58px;height:58px;border-radius:50%;background:#e7f7ec;color:#16803d;display:grid;place-items:center;margin:0 auto 18px;font-size:30px;font-weight:900}.success p{color:#66736b;line-height:1.6}@media(max-width:600px){.card{padding:21px}.two{grid-template-columns:1fr}.gallery{grid-template-columns:repeat(2,1fr)}}`;