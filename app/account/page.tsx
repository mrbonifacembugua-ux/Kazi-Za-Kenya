"use client";

import { ChangeEvent, useEffect, useState } from "react";
import { useRouter } from "next/navigation";
import { supabase } from "../../lib/supabase";

type Profile={full_name:string|null;role:string|null;area:string|null;avatar_url:string|null;profile_photo_url:string|null;bio:string|null;years_experience:number|null;country_code:string|null};
type Job={id:string;title:string;status:string;created_at:string;customer_id:string;taken_by:string|null};
type Review={rating:number;reviewee_id:string};
function countryName(code:string|null|undefined){if(!code)return"";try{return new Intl.DisplayNames(["en"],{type:"region"}).of(code.toUpperCase())||code.toUpperCase()}catch{return code.toUpperCase()}}

export default function AccountPage(){
 const router=useRouter();
 const [loading,setLoading]=useState(true),[busy,setBusy]=useState(false),[deleting,setDeleting]=useState(false),[saving,setSaving]=useState(false),[uploading,setUploading]=useState(false);
 const [profile,setProfile]=useState<Profile|null>(null),[jobs,setJobs]=useState<Job[]>([]),[uid,setUid]=useState(""),[rating,setRating]=useState({avg:0,count:0}),[unreadCount,setUnreadCount]=useState(0),[ratedJobs,setRatedJobs]=useState<Set<string>>(new Set()),[accountError,setAccountError]=useState(""),[accountSuccess,setAccountSuccess]=useState("");
 const [fullName,setFullName]=useState(""),[area,setArea]=useState(""),[role,setRole]=useState("both"),[bio,setBio]=useState(""),[yearsExperience,setYearsExperience]=useState(""),[avatarUrl,setAvatarUrl]=useState("");
 useEffect(()=>{void load();const ch=supabase.channel("account-unread-live").on("postgres_changes",{event:"INSERT",schema:"public",table:"messages"},()=>void refreshUnreadCount()).subscribe();return()=>{supabase.removeChannel(ch)}},[]);
 async function refreshUnreadCount(){const{data,error}=await supabase.rpc("get_unread_message_count");if(!error)setUnreadCount(Number(data)||0)}
 async function load(){
  const{data:{user}}=await supabase.auth.getUser();if(!user){router.replace("/login?next=%2Faccount");return}setUid(user.id);
  const[{data:p},{data:posted},{data:work},{data:reviews},{data:unread},{data:mine}]=await Promise.all([
   supabase.from("profiles").select("full_name,role,area,avatar_url,profile_photo_url,bio,years_experience,country_code").eq("id",user.id).maybeSingle(),
   supabase.from("jobs").select("id,title,status,created_at,customer_id,taken_by").eq("customer_id",user.id).order("created_at",{ascending:false}),
   supabase.from("jobs").select("id,title,status,created_at,customer_id,taken_by").eq("taken_by",user.id).order("created_at",{ascending:false}),
   supabase.from("reviews").select("rating,reviewee_id").eq("reviewee_id",user.id),supabase.rpc("get_unread_message_count"),supabase.from("reviews").select("job_id").eq("reviewer_id",user.id)
  ]);
  const pp=(p||null) as Profile|null;setProfile(pp);setFullName(pp?.full_name||"");setArea(pp?.area||"");setRole(pp?.role||"both");setBio(pp?.bio||"");setYearsExperience(pp?.years_experience==null?"":String(pp.years_experience));setAvatarUrl(pp?.profile_photo_url||pp?.avatar_url||"");
  const all=[...(posted||[]),...(work||[])];setJobs(Array.from(new Map(all.map(j=>[j.id,j])).values()));const rr=(reviews||[]) as Review[];setRating({count:rr.length,avg:rr.length?rr.reduce((a,r)=>a+Number(r.rating),0)/rr.length:0});setUnreadCount(Number(unread)||0);setRatedJobs(new Set((mine||[]).map((r:any)=>r.job_id)));setLoading(false)
 }
 async function logout(){setBusy(true);setAccountError("");const{error}=await supabase.auth.signOut();if(error){setAccountError(error.message);setBusy(false);return}router.replace("/login");router.refresh()}
 async function uploadAvatar(event:ChangeEvent<HTMLInputElement>){const file=event.target.files?.[0];if(!file||!uid)return;setAccountError("");setAccountSuccess("");if(!file.type.startsWith("image/")){setAccountError("Please choose an image file.");return}if(file.size>5*1024*1024){setAccountError("Profile photo must be 5 MB or smaller.");return}setUploading(true);const ext=(file.name.split(".").pop()||"jpg").toLowerCase().replace(/[^a-z0-9]/g,"")||"jpg";const path=`profiles/${uid}/avatar.${ext}`;const{error:uploadError}=await supabase.storage.from("kazi-media").upload(path,file,{upsert:true,contentType:file.type,cacheControl:"3600"});if(uploadError){setAccountError(uploadError.message);setUploading(false);return}const{data}=supabase.storage.from("kazi-media").getPublicUrl(path);const url=`${data.publicUrl}?v=${Date.now()}`;setAvatarUrl(url);setUploading(false);setAccountSuccess("Photo uploaded. Click Save profile changes to keep it on your profile.")}
 async function saveProfile(){if(!uid)return;setSaving(true);setAccountError("");setAccountSuccess("");const cleanName=fullName.trim();if(!cleanName){setAccountError("Please enter your name.");setSaving(false);return}const years=yearsExperience.trim()===""?null:Number(yearsExperience);if(years!==null&&(!Number.isFinite(years)||years<0||years>80)){setAccountError("Years of experience must be between 0 and 80.");setSaving(false);return}const update={full_name:cleanName,area:area.trim()||null,role,bio:bio.trim()||null,years_experience:years,avatar_url:avatarUrl||null,profile_photo_url:avatarUrl||null};const{error}=await supabase.from("profiles").update(update).eq("id",uid);if(error){setAccountError(error.message);setSaving(false);return}setProfile(prev=>({...prev,...update,country_code:prev?.country_code||null} as Profile));setAccountSuccess("Profile updated successfully.");setSaving(false);router.refresh()}
 async function deleteAccount(){
  setAccountError("");setAccountSuccess("");
  const confirmation=window.prompt('This permanently deletes your AnyDayWork account. Type DELETE to continue.');
  if(confirmation!=="DELETE")return;
  if(!window.confirm("Final confirmation: permanently delete this account? This cannot be undone."))return;
  setDeleting(true);
  const{data,error}=await supabase.functions.invoke("delete-my-account",{body:{confirm:"DELETE"}});
  if(error||!data?.ok){setAccountError(data?.error||error?.message||"We could not delete your account. Please try again.");setDeleting(false);return}
  try{await supabase.auth.signOut({scope:"local"})}catch{}
  router.replace("/login?deleted=1");router.refresh();
 }
 const active=jobs.filter(j=>!["completed","archived","cancelled"].includes(j.status)),history=jobs.filter(j=>["completed","archived","cancelled"].includes(j.status)),locationLabel=[profile?.area,countryName(profile?.country_code)].filter(Boolean).join(", ")||"Location not set";
 if(loading)return <main className="page"><section className="card"><h1>Loading your account…</h1></section><style jsx>{styles}</style></main>;
 return <main className="page"><section className="card"><div className="topline"><button className="back" onClick={()=>router.push("/")}>← Marketplace</button><button className="topLogout" disabled={busy||deleting} onClick={logout}>{busy?"Signing out…":"↪ Sign out"}</button></div><header><div>{avatarUrl?<img className="avatar" src={avatarUrl} alt="Profile"/>:<div className="avatar blank">👤</div>}</div><div><h1>{profile?.full_name||"My AnyDayWork account"}</h1><p>{locationLabel} · {profile?.role||"member"}</p>{rating.count>0&&<strong>⭐ {rating.avg.toFixed(1)} ({rating.count} verified review{rating.count===1?"":"s"})</strong>}</div></header>
 <section className="profileEditor"><div className="editorHeading"><div><h2>Edit profile</h2><p>Keep your public profile information up to date.</p></div></div><div className="photoRow"><div>{avatarUrl?<img className="avatar large" src={avatarUrl} alt="Profile preview"/>:<div className="avatar large blank">👤</div>}</div><div><label className="uploadButton">{uploading?"Uploading…":"📷 Upload profile photo"}<input type="file" accept="image/*" onChange={uploadAvatar} disabled={uploading||saving||deleting}/></label><small>JPG, PNG or WebP. Maximum 5 MB.</small></div></div><div className="formGrid"><label>Display name<input value={fullName} onChange={e=>setFullName(e.target.value)} maxLength={80}/></label><label>Area<input value={area} onChange={e=>setArea(e.target.value)} maxLength={80} placeholder="e.g. Kingston"/></label><label>Account role<select value={role} onChange={e=>setRole(e.target.value)}><option value="both">Both</option><option value="customer">Employer</option><option value="provider">Worker / service provider</option></select></label><label>Years of experience<input type="number" min="0" max="80" value={yearsExperience} onChange={e=>setYearsExperience(e.target.value)} placeholder="Optional"/></label><label className="wide">About you<textarea value={bio} onChange={e=>setBio(e.target.value)} maxLength={500} rows={4} placeholder="Tell people about your skills or the work you need help with."/></label></div><button className="saveProfile" disabled={saving||uploading||deleting} onClick={saveProfile}>{saving?"Saving…":"Save profile changes"}</button></section>
 <div className="actions"><button onClick={()=>router.push("/post-job")}>＋ Post a job</button><button onClick={()=>router.push("/offer-service")}>🛠 Offer a service</button><button onClick={()=>router.push("/manage-jobs")}>📋 Manage my jobs</button><button onClick={()=>router.push("/my-work")}>🔨 My work</button><button onClick={()=>router.push("/messages")}>💬 Messages{unreadCount?` (${unreadCount})`:""}</button></div><h2>Active jobs</h2>{active.length===0?<p className="empty">No active jobs right now.</p>:<div className="list">{active.map(j=><JobRow key={j.id} job={j} uid={uid} router={router} rated={ratedJobs.has(j.id)}/>)}</div>}<h2>Job history</h2>{history.length===0?<p className="empty">Completed and archived jobs will appear here.</p>:<div className="list">{history.map(j=><JobRow key={j.id} job={j} uid={uid} router={router} rated={ratedJobs.has(j.id)}/>)}</div>}{accountError&&<p className="error" role="alert">{accountError}</p>}{accountSuccess&&<p className="success" role="status">{accountSuccess}</p>}<section className="danger"><h2>Delete account</h2><p>Permanently delete your AnyDayWork account. This cannot be undone.</p><button className="delete" disabled={busy||deleting||saving||uploading} onClick={deleteAccount}>{deleting?"Deleting account…":"Delete account"}</button></section></section><style jsx>{styles}</style></main>
}
function JobRow({job,uid,router,rated}:{job:Job;uid:string;router:any;rated:boolean}){const employer=job.customer_id===uid,done=["completed","archived"].includes(job.status);return <article><div><b>{job.title}</b><p>{employer?"You posted this job":"You were hired for this job"} · {new Date(job.created_at).toLocaleDateString()}</p></div><div className="right"><span>{job.status.replaceAll("_"," ")}</span>{done&&!rated&&<button onClick={()=>router.push(`/rate-job?job=${job.id}`)}>Rate</button>}{done&&rated&&<em>✓ Rated</em>}<button onClick={()=>router.push(employer?"/manage-jobs":"/my-work")}>Open</button></div></article>}
const styles=`*{box-sizing:border-box}.page{min-height:100vh;background:#f3f6f3;padding:30px 16px;font-family:Inter,system-ui,-apple-system,"Segoe UI",sans-serif;color:#17221b}.card{max-width:900px;margin:auto;background:#fff;border:1px solid #dce4dc;border-radius:20px;padding:28px;box-shadow:0 12px 40px rgba(27,43,31,.1)}.topline{display:flex;justify-content:space-between;align-items:center;gap:12px;margin-bottom:20px}.back{border:0;background:transparent;color:#15803d;font-weight:800;cursor:pointer;padding:0}.topLogout{border:1px solid #d5ddd7;background:#fff;color:#33443a;border-radius:10px;padding:9px 14px;font-weight:850;cursor:pointer}.topLogout:disabled,.delete:disabled,.saveProfile:disabled{opacity:.55;cursor:not-allowed}header{display:flex;align-items:center;gap:16px;border-bottom:1px solid #e6ece7;padding-bottom:22px}.avatar{width:72px;height:72px;border-radius:50%;object-fit:cover;border:2px solid #dce8df}.avatar.large{width:92px;height:92px}.blank{display:grid;place-items:center;background:#eef5ef;font-size:30px}h1{margin:0 0 5px;font-size:28px}header p{margin:0 0 5px;color:#68756d}header strong{font-size:13px;color:#9a6b00}.profileEditor{margin:26px 0;padding:22px;border:1px solid #dce7de;border-radius:16px;background:#fbfdfb}.editorHeading h2{margin:0 0 4px}.editorHeading p{margin:0 0 18px;color:#68756d}.photoRow{display:flex;align-items:center;gap:18px;margin-bottom:20px}.uploadButton{display:inline-block;border:1px solid #cfe1d3;background:#f4fbf6;color:#176b35;border-radius:10px;padding:10px 13px;font-weight:850;cursor:pointer}.uploadButton input{display:none}.photoRow small{display:block;margin-top:7px;color:#758078}.formGrid{display:grid;grid-template-columns:1fr 1fr;gap:14px}.formGrid label{font-size:13px;font-weight:800;color:#34443a}.formGrid input,.formGrid select,.formGrid textarea{width:100%;margin-top:6px;border:1px solid #ccd8cf;border-radius:10px;padding:11px 12px;background:#fff;color:#17221b;font:inherit}.formGrid textarea{resize:vertical}.formGrid .wide{grid-column:1/-1}.saveProfile{margin-top:16px;border:0;background:#15803d;color:#fff;border-radius:10px;padding:11px 16px;font-weight:900;cursor:pointer}.actions{display:grid;grid-template-columns:repeat(5,1fr);gap:10px;margin:24px 0}.actions button,.right button{border:1px solid #cfe1d3;background:#f4fbf6;color:#176b35;border-radius:11px;padding:11px;font-weight:800;cursor:pointer}h2{font-size:18px;margin:28px 0 12px}.list{display:grid;gap:9px}article{display:flex;justify-content:space-between;gap:14px;align-items:center;border:1px solid #e0e7e1;border-radius:12px;padding:14px}article p{margin:4px 0 0;color:#758078;font-size:12px}.right{display:flex;align-items:center;gap:10px}.right span{text-transform:capitalize;font-size:11px;font-weight:850;background:#eef7f0;color:#2d6940;border-radius:999px;padding:6px 9px}.right button{padding:7px 10px}.right em{font-size:11px;font-style:normal;color:#647269;font-weight:800}.empty{color:#7a867e;background:#f8faf8;padding:14px;border-radius:10px}.error,.success{margin-top:20px;padding:12px;border-radius:10px;font-weight:700}.error{border:1px solid #efc6c6;background:#fff4f4;color:#9b2626}.success{border:1px solid #bfe0c8;background:#f1fbf4;color:#176b35}.danger{margin-top:34px;padding-top:24px;border-top:1px solid #eadede}.danger h2{margin:0 0 7px}.danger p{margin:0 0 15px;color:#68756d}.delete{border:1px solid #dcaeae;background:#fff5f5;color:#a51f1f;border-radius:10px;padding:10px 15px;font-weight:850;cursor:pointer}@media(max-width:700px){.card{padding:20px}.actions{grid-template-columns:1fr 1fr}.formGrid{grid-template-columns:1fr}.formGrid .wide{grid-column:auto}.photoRow{align-items:flex-start;flex-direction:column}article{align-items:flex-start;flex-direction:column}.right{width:100%;justify-content:flex-start;flex-wrap:wrap}h1{font-size:23px}.topLogout{padding:8px 11px}}`;
