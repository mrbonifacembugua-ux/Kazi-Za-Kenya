"use client";

import { useEffect } from "react";
import { createClient } from "@supabase/supabase-js";

const supabase=createClient("https://pnqmqxeuzcodnxdixnvc.supabase.co","sb_publishable_GWBhAF05Qg7mEsqzjKfxJQ_HmyNsn3l");

export default function LiveProvidersEnhancer(){useEffect(()=>{let stopped=false;let timer:number|undefined;let layer:any=null;let lastSignature="";const known:Record<string,[number,number]>={kilimani:[-1.2928,36.7877],kileleshwa:[-1.2857,36.777],lavington:[-1.2815,36.769],westlands:[-1.2676,36.807],"south b":[-1.3074,36.831],"south c":[-1.305,36.82],kasarani:[-1.2215,36.897],eastleigh:[-1.275,36.85],donholm:[-1.299,36.891],embakasi:[-1.323,36.902],langata:[-1.329,36.781],"lang'ata":[-1.329,36.781]};const geocode=async(area:string,road:string,county:string):Promise<[number,number]|null>=>{const key=(area||"").toLowerCase().trim();if(known[key])return known[key];try{const r=await fetch(`https://nominatim.openstreetmap.org/search?format=jsonv2&limit=1&countrycodes=ke&q=${encodeURIComponent([road,area,county||"Nairobi","Kenya"].filter(Boolean).join(", "))}`,{headers:{Accept:"application/json"}});const a=await r.json();if(!a?.length)return null;const lat=Number(a[0].lat),lng=Number(a[0].lon);return Number.isFinite(lat)&&Number.isFinite(lng)?[lat,lng]:null}catch{return null}};const capture=(L:any)=>{if((window as any).__kaziMap)return;const originalMap=L.map;L.map=function(...args:any[]){const m=originalMap.apply(this,args);(window as any).__kaziMap=m;return m};const originalAddLayer=L.Map.prototype.addLayer;L.Map.prototype.addLayer=function(l:any){const r=originalAddLayer.call(this,l);(window as any).__kaziMap=this;return r}};async function render(){if(stopped||window.location.pathname!=="/")return;const L=(window as any).L;if(!L){timer=window.setTimeout(render,250);return}capture(L);const map=(window as any).__kaziMap;if(!map){timer=window.setTimeout(render,250);return}const active=document.querySelector<HTMLElement>(".main-tab.active")?.textContent||"";if(!active.toLowerCase().includes("find a worker")){layer?.clearLayers();lastSignature="";return}const{data:profiles}=await supabase.from("profiles").select("id,full_name,area,road,county,latitude,longitude,is_active,role,profile_photo_url,created_at").eq("role","provider").eq("is_active",true).order("created_at",{ascending:false}).limit(100);if(stopped)return;const ids=(profiles||[]).map((p:any)=>p.id);const[{data:services}]=await Promise.all([ids.length?supabase.from("services").select("id,provider_id,title,description,price_from,price_to,availability_status,created_at").in("provider_id",ids).order("created_at",{ascending:false}):Promise.resolve({data:[]})]);const serviceMap=new Map<string,any>();(services||[]).forEach((s:any)=>{if(!serviceMap.has(s.provider_id))serviceMap.set(s.provider_id,s)});const live:any[]=[];for(const p of profiles as any[]){const s=serviceMap.get(p.id);if(!s)continue;let coords:[number,number]|null=null;if(Number.isFinite(Number(p.latitude))&&Number.isFinite(Number(p.longitude)))coords=[Number(p.latitude),Number(p.longitude)];else coords=await geocode(p.area||"",p.road||"",p.county||"Nairobi");if(coords)live.push({p,s,coords})}const signature=JSON.stringify(live.map(x=>[x.p.id,x.coords[0],x.coords[1],x.s.id]));if(signature===lastSignature)return;lastSignature=signature;if(!layer)layer=L.layerGroup().addTo(map);layer.clearLayers();const icon=L.divIcon({className:"kazi-live-provider-marker",html:`<div style="width:38px;height:38px;border-radius:50% 50% 50% 0;transform:rotate(-45deg);background:#16803d;border:3px solid white;box-shadow:0 3px 10px #0005;display:grid;place-items:center"><span style="transform:rotate(45deg);font-size:18px">👷</span></div>`,iconSize:[38,38],iconAnchor:[19,38]});live.forEach(({p,coords}:any)=>{const marker=L.marker(coords,{icon,title:p.full_name||"Kazi za Kenya provider",zIndexOffset:3000});marker.bindTooltip(`<b>${p.full_name||"Kazi za Kenya provider"}</b><br>📍 ${p.area||p.county||"Kenya"}<br><small>Click to open profile</small>`,{direction:"top",offset:[0,-34]});marker.on("click",()=>{map.flyTo(coords,17,{animate:true,duration:.6});window.location.assign(`/profile/${encodeURIComponent(String(p.id))}`)});marker.addTo(layer)})}void render();timer=window.setInterval(()=>void render(),3000);return()=>{stopped=true;if(timer)window.clearInterval(timer);layer?.clearLayers()};},[]);return null}
